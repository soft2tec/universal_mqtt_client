platforms: [vm, chrome, firefox]

on_os:
  mac-os:
    platforms: [vm, chrome, firefox, safari]

override_platforms:
  chrome:
    settings:
      arguments: --no-sandbox
  firefox:
    settings:
      arguments: -headless
